# Explicit optional local vision adapter target; not a complete platform dependency lock.
# No model weights are installed by these requirements.
# `rapidocr` itself is installed with `--no-deps` (scripts/package-python.mjs): its published
# metadata pins opencv-python, but the worker imports only `cv2` non-GUI functions, so the
# headless wheel replaces it. Its real runtime dependencies are the pinned list below.
opencv-python-headless==5.0.0.93
onnxruntime==1.30.0
numpy==2.5.3
pyclipper==1.4.0
shapely==2.1.2
omegaconf==2.3.1
pillow==12.3.0
tqdm==4.70.1
requests==2.34.2
PyYAML==6.0.3
colorlog==6.12.0
six==1.17.0
