# Explicit optional local vision adapter target; not a complete platform dependency lock.
# No model weights are installed by these requirements.
# The worker's vision runtime check looks for numpy, cv2, onnxruntime and rapidocr; ranges rather
# than exact pins, because a pin can have no wheel for every supported Python (see the translation
# requirements for the same lesson). Install exactly one OpenCV wheel in the environment.
rapidocr>=3,<4
opencv-python>=5.0.0.93,<6
onnxruntime>=1.20,<2
numpy>=2,<3
