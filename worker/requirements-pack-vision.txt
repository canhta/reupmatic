# The vision runtime pack's exclusive distributions. Shared deps (numpy, onnxruntime, requests,
# PyYAML, tqdm) stay in the base bundle; install these with --no-deps.
# `rapidocr` itself is installed with --no-deps from requirements-sources-vision.txt.
opencv-python-headless==5.0.0.93
pyclipper==1.4.0
shapely==2.1.2
omegaconf==2.3.1
antlr4-python3-runtime==4.9.3
pillow==12.3.0
colorlog==6.12.0
six==1.17.0
# rapidocr downloads/decode helpers that import requests; huggingface_hub no longer pulls it.
requests==2.34.2
urllib3==2.8.0
charset-normalizer==3.5.1
